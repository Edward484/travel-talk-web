import React from 'react';
import { useRoutes, Routes } from 'react-router';
import { BrowserRouter } from 'react-router-dom';

//TODO: Add routes
const Router = () => (
  <BrowserRouter>
    <Routes />
  </BrowserRouter>
);
export default Router;
